[project]
name = "warframe-mcp"
version = "1.1.0"
description = "MCP server for Warframe.market and WarframeStat.us APIs"
readme = "README.md"
requires-python = ">=3.12"
authors = [{ name = "snakeplisken47" }]
dependencies = [
    "httpx>=0.28.0,<1",
]

[tool.setuptools]
py-modules = ["server", "clients", "main"]
